# 韬纪元AI — 完整开发文档

> 版本：v1.1 | 日期：2026-06-18 | 基于原型 `prototype/index.html` 及 `docs/韬纪元AI_公网功能清单_20260615/`
>
> **变更记录：**
> - v1.1 (2026-06-18)：数据库由 MySQL 改为 **PostgreSQL 16**；后端（`backend/`）已完成初始实现；更新开发进度状态；补充未完成事项清单。
> - v1.0 (2026-06-17)：初版文档。

---

## 目录

1. [项目定位与边界](#1-项目定位与边界)
2. [系统整体架构](#2-系统整体架构)
3. [前端模块拆解](#3-前端模块拆解)
4. [后端模块拆解](#4-后端模块拆解)
5. [PostgreSQL 数据库表设计](#5-postgresql-数据库表设计)
6. [API 接口规范](#6-api-接口规范)
7. [AI 能力层说明](#7-ai-能力层说明)
8. [开发分工与顺序](#8-开发分工与顺序)
9. [环境与部署](#9-环境与部署)
10. [当前进度与待完成事项](#10-当前进度与待完成事项)

---

## 1. 项目定位与边界

### 1.1 核心定位

**韬纪元AI** 是一个 AI 助贷报告生产与资料作业平台，服务对象为助贷顾问、机构管理者和客户。

核心链路：
```
客户资料收集
  → AI 资料识别与缺口分析
  → 客户 360 画像
  → 顾问选择银行产品
  → AI 填写银行制式表格
  → 顾问逐字段复核
  → 导出材料包（ZIP）
  → 提交银行并记录调用
```

### 1.2 合规边界（开发必读）

| 约束 | 说明 |
|------|------|
| 不放款 | 平台不提供资金、不承诺额度 |
| 不审批 | 不替代银行及持牌金融机构的最终审核 |
| 必复核 | 所有 AI 输出结果均需顾问人工复核后才能用于提交 |
| 不盲选 | C 端客户不能自行选择银行产品，须由顾问操作 |

---

## 2. 系统整体架构

### 2.1 四端入口

```
┌─────────────────────────────────────────────────────┐
│                   韬纪元AI 平台                        │
├──────────────┬──────────────┬──────────────┬────────┤
│  B端PC工作台  │  C端客户入口  │  顾问移动端   │ 管理后台│
│ (Web/React)  │(微信小程序/H5)│(微信小程序/H5)│(Web)  │
└──────────────┴──────────────┴──────────────┴────────┘
                        │
              ┌──────────────────┐
              │   后端 REST API   │
              │  (Node.js/Java)  │
              └──────────────────┘
                        │
         ┌──────────────┼──────────────┐
         │              │              │
    ┌────────┐   ┌─────────────┐  ┌────────┐
    │ MySQL  │   │  AI 服务层   │  │ 文件存储│
    │(主数据)│   │(识别/填表/AI)│  │(OSS/COS│
    └────────┘   └─────────────┘  └────────┘
```

### 2.2 技术栈建议

| 层 | 技术选型 | 说明 |
|----|---------|------|
| B端PC前端 | React + TypeScript + Ant Design | SPA，路由用 react-router |
| C端/顾问移动端 | 微信小程序（已有 miniprogram_c 框架，Skyline 渲染） | TypeScript + glass-easel |
| 后端 | Node.js (NestJS) ✅ | RESTful API + JWT 认证，代码在 `backend/` |
| 数据库 | **PostgreSQL 16**（已由 MySQL 改为 PG） ✅ | TypeORM，迁移文件：`backend/src/database/migrations/` |
| 文件存储 | 腾讯云 COS | 客户资料、导出材料包 |
| AI 能力 | 第三方 LLM API（GPT-4/Claude/国内大模型） + OCR | 资料识别、表格填写 |
| 缓存 | Redis | 会话、任务队列状态 |
| 消息队列 | Redis Queue 或 RabbitMQ | AI 异步任务 |

---

## 3. 前端模块拆解

### 3.1 B端PC工作台（顾问/管理员）

> 左侧固定导航栏 + 右侧内容区，深色主题（#0F172A 导航栏）

| 编号 | 菜单/模块 | 核心功能 |
|------|----------|---------|
| B-01 | 今日作业（Dashboard） | 待处理任务卡片、AI 填表任务队列、快速操作入口 |
| B-02 | 客户列表 | 客户搜索/筛选/排序、新建客户、进入客户详情 |
| B-03 | 客户详情 | 四个 Tab：客户总览、客户资料、报告产出、跟进记录 |
| B-04 | 报告作业台 | AI 填表任务列表、进入报告详情、材料目录、导出材料包（ZIP） |
| B-05 | AI 资料识别 | 上传资料触发识别、查看识别结果、缺口分析报告 |
| B-06 | 银行资料管理 | 银行列表、金融产品 Tab、固定资料配置（字段要求） |
| B-07 | 平台配置 | AI 填表规则管理、来源指示图、复核规则 |
| B-08 | 会员与套餐 | 当前套餐状态、用量统计、升级套餐申请 |
| B-09 | 调用记录 | 报告/材料整理调用日志、按月/客户筛选 |
| B-10 | 机构账号 | 员工账号列表、新增员工、权限配置（角色/数据范围/功能权限） |
| B-11 | API 配置 | API Key 管理（占位，企业版功能） |
| B-12 | AI 助手面板 | 右侧浮动面板，支持自然语言查客户、生成补件清单 |

**客户详情四个 Tab 字段说明：**

| Tab | 内容 |
|-----|------|
| 客户总览 | 融资需求摘要、标签体系、风险关注点、资料完整度进度条、AI 画像摘要 |
| 客户资料 | 已上传资料列表（营业执照/流水/征信/税票/不动产等）、识别状态、缺口提示 |
| 报告产出 | AI 填表任务列表、制式表格草稿、来源指示图、字段复核状态 |
| 跟进记录 | 时间线形式记录顾问操作、客户补件记录、提交银行记录 |

### 3.2 C端客户入口（微信小程序/H5）

> 客户自助操作界面，不允许客户自行选择银行产品

| 编号 | 页面 | 核心功能 |
|------|------|---------|
| C-01 | 首页/AI 对话 | 融资诉求输入、AI 引导对话、产品方向参考（仅参考，不承诺） |
| C-02 | 资料上传 | 拍照/上传营业执照、流水、税票、授权书等 |
| C-03 | 办理进度 | 查看当前资料完整度、顾问反馈、补件提示 |
| C-04 | 联系顾问 | 顾问联系方式展示（微信/电话，Mock 演示） |
| C-05 | 授权管理 | 征信授权签署、查看已授权记录 |
| C-06 | 历史对话 | 与 AI 的历史对话记录 |
| C-07 | 我的资料 | 所有已上传资料管理、资料状态 |

### 3.3 顾问移动端（微信小程序）

> 外勤作业场景，轻量化操作

| 编号 | 页面 | 核心功能 |
|------|------|---------|
| M-01 | 顾问 AI 对话 | 快速查询客户信息、生成补件清单 |
| M-02 | 客户选择 | 顶部客户切换条、快速切换当前操作客户 |
| M-03 | 客户列表 | 简化版客户列表、外勤跟进状态 |
| M-04 | 银行材料整理 | 按客户查看银行材料要求、快速发起 AI 资料整理 |
| M-05 | AI 资料识别 | 拍照识别客户资料、返回摘要和缺口 |
| M-06 | 我的 | 顾问个人信息、用量查看 |

---

## 4. 后端模块拆解

### 4.1 模块清单

| 编号 | 模块名 | 说明 |
|------|-------|------|
| S-01 | 认证与账号 | 登录/注册、JWT token、微信 openid 绑定 |
| S-02 | 机构与权限 | 机构管理、员工账号、角色/数据范围/功能权限 |
| S-03 | 客户管理 | 客户 CRUD、标签、360 画像聚合、跟进记录 |
| S-04 | 资料管理 | 客户资料上传（COS）、资料状态管理、缺口分析 |
| S-05 | AI 识别服务 | 触发 AI 识别、异步任务管理、结果回写 |
| S-06 | 报告作业台 | AI 填表任务创建/管理、字段草稿、复核流程 |
| S-07 | 银行资料管理 | 银行/产品 CRUD、固定资料字段配置 |
| S-08 | 材料包导出 | 生成 ZIP（表格+资料+报告）、记录导出调用 |
| S-09 | 会员套餐 | 套餐定义、机构订阅、调用次数管理 |
| S-10 | 调用记录 | 每次 AI 调用日志、扣次、查询统计 |
| S-11 | 平台配置 | AI 填表规则、复核规则、模板管理 |
| S-12 | API 能力（占位）| 对外 API Key 管理、调用鉴权（企业版预留） |
| S-13 | 通知 | 站内消息、补件提醒、微信模板消息 |

---

## 5. PostgreSQL 数据库表设计

> **注意：** 数据库已改为 **PostgreSQL 16**，以下 DDL 为原始设计参考。实际可执行的 PostgreSQL 建表脚本见 `backend/src/database/migrations/001-initial-schema.sql`（含所有 ENUM 类型定义、PG 语法转换）。
>
> 时间字段使用 `TIMESTAMP`；软删除用 `deleted_at`（TypeORM `@DeleteDateColumn()`）

### 5.1 账号与权限

```sql
-- 机构表
CREATE TABLE institutions (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  name          VARCHAR(100)    NOT NULL COMMENT '机构名称',
  plan_id       INT UNSIGNED    NOT NULL DEFAULT 1 COMMENT '当前套餐ID',
  quota_total   INT             NOT NULL DEFAULT 30 COMMENT '当月总配额',
  quota_used    INT             NOT NULL DEFAULT 0  COMMENT '当月已用次数',
  quota_reset_at DATE           COMMENT '配额重置日期',
  status        TINYINT         NOT NULL DEFAULT 1 COMMENT '1=启用 0=停用',
  created_at    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  deleted_at    DATETIME        DEFAULT NULL
) ENGINE=InnoDB COMMENT='机构表';

-- 用户表（顾问/主管/运营管理员）
CREATE TABLE users (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  institution_id BIGINT UNSIGNED NOT NULL COMMENT '所属机构',
  name          VARCHAR(50)     NOT NULL COMMENT '姓名',
  phone         VARCHAR(20)     UNIQUE NOT NULL COMMENT '手机号（登录账号）',
  password_hash VARCHAR(255)    NOT NULL COMMENT 'bcrypt',
  wx_openid     VARCHAR(64)     DEFAULT NULL COMMENT '微信小程序openid',
  wx_unionid    VARCHAR(64)     DEFAULT NULL,
  role          ENUM('advisor','supervisor','admin') NOT NULL DEFAULT 'advisor',
  data_scope    ENUM('self','team','all')            NOT NULL DEFAULT 'self'
                COMMENT '仅本人/本团队/全部客户',
  status        TINYINT         NOT NULL DEFAULT 1 COMMENT '1=启用 0=停用',
  last_login_at DATETIME        DEFAULT NULL,
  created_at    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  deleted_at    DATETIME        DEFAULT NULL,
  INDEX idx_institution (institution_id),
  INDEX idx_phone (phone)
) ENGINE=InnoDB COMMENT='系统用户表';

-- 用户功能权限表（精细权限）
CREATE TABLE user_permissions (
  id          BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  user_id     BIGINT UNSIGNED NOT NULL,
  permission  VARCHAR(50)     NOT NULL
              COMMENT '如: view_customer, edit_followup, ai_parse, maintain_bank_material, manage_plan, manage_account, config_permission',
  INDEX idx_user (user_id)
) ENGINE=InnoDB COMMENT='用户功能权限表';
```

### 5.2 客户数据

```sql
-- 客户表
CREATE TABLE customers (
  id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  institution_id  BIGINT UNSIGNED NOT NULL,
  advisor_id      BIGINT UNSIGNED NOT NULL COMMENT '负责顾问',
  name            VARCHAR(100)    NOT NULL COMMENT '企业名称或个人姓名',
  contact_name    VARCHAR(50)     DEFAULT NULL COMMENT '联系人',
  contact_phone   VARCHAR(20)     DEFAULT NULL,
  wx_openid       VARCHAR(64)     DEFAULT NULL COMMENT 'C端微信openid',
  financing_need  TEXT            DEFAULT NULL COMMENT '融资诉求描述',
  loan_amount     DECIMAL(15,2)   DEFAULT NULL COMMENT '意向融资金额（万元）',
  loan_purpose    VARCHAR(255)    DEFAULT NULL COMMENT '资金用途',
  status          ENUM('collecting','reviewing','reporting','submitted','done','paused')
                  NOT NULL DEFAULT 'collecting'
                  COMMENT '资料收集中/复核中/报告生产/已提交银行/完成/暂停',
  doc_completeness TINYINT UNSIGNED DEFAULT 0 COMMENT '资料完整度 0-100',
  ai_summary      TEXT            DEFAULT NULL COMMENT 'AI生成的客户画像摘要',
  risk_notes      TEXT            DEFAULT NULL COMMENT '风险关注点',
  created_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  deleted_at      DATETIME        DEFAULT NULL,
  INDEX idx_institution (institution_id),
  INDEX idx_advisor (advisor_id),
  INDEX idx_status (status)
) ENGINE=InnoDB COMMENT='客户表';

-- 客户标签表
CREATE TABLE customer_labels (
  id          BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  customer_id BIGINT UNSIGNED NOT NULL,
  label       VARCHAR(50)     NOT NULL COMMENT '如: 经营稳定、流水良好、有不动产',
  label_type  ENUM('auto','manual') DEFAULT 'auto',
  INDEX idx_customer (customer_id)
) ENGINE=InnoDB COMMENT='客户标签';

-- 客户跟进记录表
CREATE TABLE follow_up_records (
  id          BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  customer_id BIGINT UNSIGNED NOT NULL,
  advisor_id  BIGINT UNSIGNED NOT NULL,
  type        ENUM('note','supplement_request','bank_submit','bank_feedback','system')
              NOT NULL DEFAULT 'note',
  content     TEXT            NOT NULL,
  created_at  DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_customer (customer_id)
) ENGINE=InnoDB COMMENT='跟进记录';

-- 客户授权记录
CREATE TABLE customer_authorizations (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  customer_id   BIGINT UNSIGNED NOT NULL,
  auth_type     ENUM('credit_check','data_use','third_party') NOT NULL,
  signed_at     DATETIME        DEFAULT NULL,
  expired_at    DATETIME        DEFAULT NULL,
  file_url      VARCHAR(500)    DEFAULT NULL COMMENT '授权书文件URL',
  status        ENUM('pending','signed','expired','revoked') DEFAULT 'pending',
  created_at    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_customer (customer_id)
) ENGINE=InnoDB COMMENT='客户授权记录';
```

### 5.3 资料管理

```sql
-- 客户资料文件表
CREATE TABLE customer_documents (
  id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  customer_id     BIGINT UNSIGNED NOT NULL,
  uploader_id     BIGINT UNSIGNED DEFAULT NULL COMMENT 'NULL=客户自传',
  uploader_type   ENUM('customer','advisor') DEFAULT 'customer',
  doc_type        ENUM(
                    'business_license',  -- 营业执照
                    'bank_statement',    -- 银行流水
                    'credit_report',     -- 征信报告
                    'tax_invoice',       -- 税票
                    'property_cert',     -- 不动产证
                    'id_card',           -- 身份证
                    'financial_statement',-- 财务报表
                    'other'
                  ) NOT NULL DEFAULT 'other',
  file_name       VARCHAR(255)    NOT NULL,
  file_url        VARCHAR(500)    NOT NULL COMMENT 'COS/OSS URL',
  file_size       INT             DEFAULT NULL COMMENT '字节',
  mime_type       VARCHAR(50)     DEFAULT NULL,
  ai_parse_status ENUM('pending','processing','done','failed') DEFAULT 'pending',
  ai_parsed_at    DATETIME        DEFAULT NULL,
  created_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  deleted_at      DATETIME        DEFAULT NULL,
  INDEX idx_customer (customer_id),
  INDEX idx_type (doc_type)
) ENGINE=InnoDB COMMENT='客户上传资料';

-- AI 识别结果表（每份资料对应多个字段）
CREATE TABLE ai_recognition_results (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  document_id   BIGINT UNSIGNED NOT NULL,
  customer_id   BIGINT UNSIGNED NOT NULL,
  field_key     VARCHAR(100)    NOT NULL COMMENT '字段名，如 company_name',
  field_label   VARCHAR(100)    NOT NULL COMMENT '字段中文名',
  field_value   TEXT            DEFAULT NULL,
  confidence    DECIMAL(5,4)    DEFAULT NULL COMMENT '置信度 0-1',
  status        ENUM('ok','missing','abnormal','needs_review') DEFAULT 'ok',
  note          VARCHAR(255)    DEFAULT NULL COMMENT 'AI标注说明',
  created_at    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_document (document_id),
  INDEX idx_customer (customer_id)
) ENGINE=InnoDB COMMENT='AI资料识别结果';

-- AI 识别任务表（异步队列）
CREATE TABLE ai_recognition_tasks (
  id            BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  institution_id BIGINT UNSIGNED NOT NULL,
  customer_id   BIGINT UNSIGNED NOT NULL,
  trigger_user_id BIGINT UNSIGNED NOT NULL,
  scope         ENUM('single_doc','all_docs','reparse') DEFAULT 'all_docs',
  document_ids  JSON            DEFAULT NULL COMMENT '指定文件IDs，NULL=全部',
  status        ENUM('queued','processing','done','failed') DEFAULT 'queued',
  result_summary TEXT           DEFAULT NULL COMMENT '缺口摘要',
  started_at    DATETIME        DEFAULT NULL,
  finished_at   DATETIME        DEFAULT NULL,
  created_at    DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_customer (customer_id)
) ENGINE=InnoDB COMMENT='AI识别任务';
```

### 5.4 银行与产品

```sql
-- 银行表
CREATE TABLE banks (
  id          INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  name        VARCHAR(100)    NOT NULL COMMENT '银行名称',
  short_name  VARCHAR(20)     DEFAULT NULL,
  logo_url    VARCHAR(500)    DEFAULT NULL,
  sort_order  INT             DEFAULT 0,
  status      TINYINT         DEFAULT 1,
  created_at  DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB COMMENT='银行表';

-- 银行金融产品表
CREATE TABLE bank_products (
  id              INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  bank_id         INT UNSIGNED    NOT NULL,
  name            VARCHAR(100)    NOT NULL COMMENT '产品名称，如：XX信用贷',
  product_type    ENUM('credit','mortgage','business','other') DEFAULT 'business',
  loan_min        DECIMAL(15,2)   DEFAULT NULL COMMENT '最低贷款额（万元）',
  loan_max        DECIMAL(15,2)   DEFAULT NULL COMMENT '最高贷款额（万元）',
  rate_min        DECIMAL(5,4)    DEFAULT NULL COMMENT '参考利率（年化）',
  description     TEXT            DEFAULT NULL,
  requirements    TEXT            DEFAULT NULL COMMENT '准入要求说明',
  sort_order      INT             DEFAULT 0,
  status          TINYINT         DEFAULT 1,
  created_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_bank (bank_id)
) ENGINE=InnoDB COMMENT='银行金融产品';

-- 银行产品固定资料配置（制式表格字段要求）
CREATE TABLE bank_material_configs (
  id              INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  product_id      INT UNSIGNED    NOT NULL,
  field_key       VARCHAR(100)    NOT NULL COMMENT '字段标识',
  field_label     VARCHAR(100)    NOT NULL COMMENT '字段中文名',
  field_type      ENUM('text','number','date','enum','file','boolean') DEFAULT 'text',
  required        TINYINT         DEFAULT 1,
  source_hint     VARCHAR(255)    DEFAULT NULL COMMENT '数据来源提示，如：来自营业执照',
  review_required TINYINT         DEFAULT 1 COMMENT '是否强制顾问复核',
  sort_order      INT             DEFAULT 0,
  INDEX idx_product (product_id)
) ENGINE=InnoDB COMMENT='银行产品固定资料字段配置';
```

### 5.5 报告作业台（AI 填表）

```sql
-- AI 填表任务表
CREATE TABLE report_tasks (
  id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  institution_id  BIGINT UNSIGNED NOT NULL,
  customer_id     BIGINT UNSIGNED NOT NULL,
  advisor_id      BIGINT UNSIGNED NOT NULL,
  product_id      INT UNSIGNED    NOT NULL COMMENT '目标银行产品',
  status          ENUM('pending','ai_filling','ai_done','reviewing','review_done','exporting','exported','submitted')
                  NOT NULL DEFAULT 'pending',
  ai_fill_at      DATETIME        DEFAULT NULL,
  reviewed_at     DATETIME        DEFAULT NULL,
  exported_at     DATETIME        DEFAULT NULL,
  submitted_at    DATETIME        DEFAULT NULL COMMENT '提交银行时间',
  export_url      VARCHAR(500)    DEFAULT NULL COMMENT '导出材料包URL',
  issue_count     INT             DEFAULT 0 COMMENT 'AI标记的问题字段数',
  created_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  INDEX idx_customer (customer_id),
  INDEX idx_advisor (advisor_id),
  INDEX idx_status (status)
) ENGINE=InnoDB COMMENT='AI填表任务（报告作业台核心表）';

-- 填表字段草稿表
CREATE TABLE report_field_drafts (
  id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  task_id         BIGINT UNSIGNED NOT NULL,
  field_key       VARCHAR(100)    NOT NULL,
  field_label     VARCHAR(100)    NOT NULL,
  ai_value        TEXT            DEFAULT NULL COMMENT 'AI预填值',
  final_value     TEXT            DEFAULT NULL COMMENT '顾问确认后的最终值',
  source_doc_id   BIGINT UNSIGNED DEFAULT NULL COMMENT '来源资料文件ID',
  source_hint     VARCHAR(255)    DEFAULT NULL COMMENT '来源说明',
  ai_status       ENUM('ok','issue','missing','needs_review') DEFAULT 'ok',
  ai_note         VARCHAR(255)    DEFAULT NULL COMMENT 'AI标注问题说明',
  review_status   ENUM('pending','approved','corrected','rejected') DEFAULT 'pending',
  reviewed_by     BIGINT UNSIGNED DEFAULT NULL,
  reviewed_at     DATETIME        DEFAULT NULL,
  INDEX idx_task (task_id)
) ENGINE=InnoDB COMMENT='填表字段草稿';
```

### 5.6 会员套餐与调用记录

```sql
-- 套餐定义表
CREATE TABLE membership_plans (
  id            INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  name          VARCHAR(50)     NOT NULL COMMENT '免费版/Plus版/机构版/旗舰版',
  monthly_quota INT             NOT NULL COMMENT '每月调用次数，-1=不限',
  max_advisors  INT             NOT NULL DEFAULT 1 COMMENT '最多员工数，-1=不限',
  features      JSON            DEFAULT NULL COMMENT '功能权益JSON',
  price_monthly DECIMAL(10,2)   DEFAULT 0 COMMENT '月付价格（元）',
  sort_order    INT             DEFAULT 0,
  is_active     TINYINT         DEFAULT 1
) ENGINE=InnoDB COMMENT='套餐定义';

-- 机构套餐订阅
CREATE TABLE institution_subscriptions (
  id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  institution_id  BIGINT UNSIGNED NOT NULL,
  plan_id         INT UNSIGNED    NOT NULL,
  started_at      DATETIME        NOT NULL,
  expired_at      DATETIME        DEFAULT NULL COMMENT 'NULL=永久/按月续',
  status          ENUM('active','expired','cancelled') DEFAULT 'active',
  created_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_institution (institution_id)
) ENGINE=InnoDB COMMENT='机构套餐订阅';

-- 调用记录表（每次消耗配额均记录）
CREATE TABLE call_records (
  id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  institution_id  BIGINT UNSIGNED NOT NULL,
  user_id         BIGINT UNSIGNED NOT NULL,
  customer_id     BIGINT UNSIGNED DEFAULT NULL,
  task_id         BIGINT UNSIGNED DEFAULT NULL,
  call_type       ENUM('ai_recognition','report_fill','doc_export','api_call')
                  NOT NULL COMMENT '调用类型',
  quota_cost      INT             NOT NULL DEFAULT 1 COMMENT '消耗次数',
  status          ENUM('success','failed') DEFAULT 'success',
  detail          VARCHAR(255)    DEFAULT NULL,
  created_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_institution_date (institution_id, created_at),
  INDEX idx_user (user_id)
) ENGINE=InnoDB COMMENT='调用记录';
```

### 5.7 平台配置

```sql
-- AI 填表规则表
CREATE TABLE ai_fill_rules (
  id              INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  institution_id  BIGINT UNSIGNED DEFAULT NULL COMMENT 'NULL=平台级规则',
  name            VARCHAR(100)    NOT NULL COMMENT '规则名称',
  scene           ENUM('form_fill','biz_data','credit_parse','doc_export','source_diagram')
                  NOT NULL COMMENT '适用场景',
  review_policy   ENUM('advisor_confirm','amount_fields','all_fields','auto_no_review')
                  NOT NULL DEFAULT 'advisor_confirm',
  mapping_desc    TEXT            DEFAULT NULL COMMENT '映射说明',
  status          TINYINT         DEFAULT 1,
  sort_order      INT             DEFAULT 0,
  created_by      BIGINT UNSIGNED DEFAULT NULL,
  created_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  updated_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB COMMENT='AI填表规则';

-- 平台全局配置 KV 表
CREATE TABLE platform_configs (
  id          INT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  config_key  VARCHAR(100)    UNIQUE NOT NULL,
  config_val  TEXT            DEFAULT NULL,
  description VARCHAR(255)    DEFAULT NULL,
  updated_at  DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
) ENGINE=InnoDB COMMENT='平台全局配置KV';

-- API Key 表（企业版预留）
CREATE TABLE api_keys (
  id              BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  institution_id  BIGINT UNSIGNED NOT NULL,
  key_hash        VARCHAR(255)    NOT NULL COMMENT 'SHA256 hash',
  key_prefix      VARCHAR(10)     NOT NULL COMMENT '展示用前缀',
  name            VARCHAR(50)     DEFAULT NULL COMMENT '用途标注',
  status          ENUM('active','revoked') DEFAULT 'active',
  last_used_at    DATETIME        DEFAULT NULL,
  created_at      DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_institution (institution_id)
) ENGINE=InnoDB COMMENT='对外API Key';
```

### 5.8 C端客户对话

```sql
-- C端 AI 对话历史
CREATE TABLE chat_sessions (
  id          BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  customer_id BIGINT UNSIGNED NOT NULL,
  source      ENUM('c_end','advisor_mobile','advisor_pc') DEFAULT 'c_end',
  created_at  DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP
) ENGINE=InnoDB COMMENT='对话会话';

CREATE TABLE chat_messages (
  id          BIGINT UNSIGNED AUTO_INCREMENT PRIMARY KEY,
  session_id  BIGINT UNSIGNED NOT NULL,
  role        ENUM('user','assistant') NOT NULL,
  content     TEXT            NOT NULL,
  created_at  DATETIME        NOT NULL DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_session (session_id)
) ENGINE=InnoDB COMMENT='对话消息';
```

---

## 6. API 接口规范

### 6.1 通用规范

```
Base URL: /api/v1
认证: Authorization: Bearer <JWT_TOKEN>
响应格式:
{
  "code": 0,          // 0=成功，非0=错误码
  "message": "ok",
  "data": {}
}
分页参数: ?page=1&pageSize=20
```

### 6.2 核心接口列表

| 模块 | Method | Path | 说明 |
|------|--------|------|------|
| 认证 | POST | /auth/login | 手机号+密码登录 |
| 认证 | POST | /auth/wx-login | 微信小程序登录（code换openid） |
| 认证 | POST | /auth/refresh | 刷新token |
| 客户 | GET | /customers | 客户列表（支持搜索/筛选/分页） |
| 客户 | POST | /customers | 新建客户 |
| 客户 | GET | /customers/:id | 客户详情（含画像摘要） |
| 客户 | PUT | /customers/:id | 更新客户信息 |
| 客户 | GET | /customers/:id/overview | 360画像概览 |
| 客户 | GET | /customers/:id/documents | 客户资料列表 |
| 客户 | GET | /customers/:id/report-tasks | 报告任务列表 |
| 客户 | GET | /customers/:id/follow-ups | 跟进记录 |
| 客户 | POST | /customers/:id/follow-ups | 添加跟进记录 |
| 资料 | POST | /documents/upload | 上传资料（multipart，返回COS URL） |
| 资料 | DELETE | /documents/:id | 删除资料 |
| AI识别 | POST | /ai/recognize | 触发AI识别任务 |
| AI识别 | GET | /ai/recognize/:taskId | 查询识别任务状态 |
| AI识别 | GET | /customers/:id/recognition-summary | 客户资料缺口摘要 |
| 报告 | GET | /report-tasks | 报告任务列表（可按机构/顾问/状态筛选） |
| 报告 | POST | /report-tasks | 创建AI填表任务（指定客户+银行产品） |
| 报告 | GET | /report-tasks/:id | 任务详情+字段草稿 |
| 报告 | PUT | /report-tasks/:id/fields/:fieldId | 顾问修改/复核字段 |
| 报告 | POST | /report-tasks/:id/approve | 顾问确认复核通过 |
| 报告 | POST | /report-tasks/:id/export | 导出材料包（异步，返回下载URL） |
| 报告 | POST | /report-tasks/:id/submit | 标记为已提交银行 |
| 银行 | GET | /banks | 银行列表 |
| 银行 | GET | /banks/:id/products | 银行产品列表 |
| 银行 | GET | /bank-products/:id/material-config | 产品固定资料字段配置 |
| 银行 | POST/PUT/DELETE | /bank-products、/bank-material-configs | 管理员维护 |
| 会员 | GET | /institution/subscription | 当前机构套餐与用量 |
| 会员 | GET | /membership-plans | 套餐列表 |
| 会员 | POST | /institution/upgrade-request | 提交升级申请 |
| 调用记录 | GET | /call-records | 调用记录列表 |
| 账号 | GET | /institution/members | 员工列表 |
| 账号 | POST | /institution/members | 新增员工 |
| 账号 | PUT | /institution/members/:id/permissions | 编辑权限 |
| 账号 | PUT | /institution/members/:id/status | 启停账号 |
| 配置 | GET/POST/PUT/DELETE | /ai-fill-rules | AI填表规则管理 |
| C端 | POST | /c/session | 创建客户会话（微信openid识别） |
| C端 | POST | /c/chat | 发送对话消息，获取AI回复 |
| C端 | GET | /c/progress | 查看我的办理进度 |
| C端 | POST | /c/documents/upload | C端客户上传资料 |

---

## 7. AI 能力层说明

### 7.1 AI 任务类型

| 类型 | 触发场景 | 消耗配额 | 输入 | 输出 |
|------|---------|---------|------|------|
| 资料识别 | 客户上传资料后 / 顾问手动触发 | 1次/份 | 文件（图片/PDF） | 字段kv + 缺口分析 |
| 客户画像 | 首次识别完成后自动生成 | 不额外扣 | 所有识别结果 | 摘要+风险点+标签 |
| AI填表 | 顾问在报告作业台创建任务 | 1次/任务 | 客户字段+银行产品配置 | 字段草稿+问题标注 |
| 补件清单 | 顾问在AI助手面板请求 | 不扣次 | 客户资料缺口 | 自然语言补件指引 |
| 问题字段重填 | 顾问标注问题后 | 不额外扣 | 顾问标注+原草稿 | 更新后的字段值 |

### 7.2 异步任务处理模式

```
POST /ai/recognize
  → 写入 ai_recognition_tasks（status=queued）
  → 加入 Redis 队列
  → 返回 task_id

Worker 消费队列:
  → 调用 OCR/LLM API
  → 写入 ai_recognition_results
  → 更新 customer_documents.ai_parse_status = done
  → 更新 customers.doc_completeness
  → 推送进度通知

前端轮询 GET /ai/recognize/:taskId 或 WebSocket 推送
```

---

## 8. 开发分工与顺序

### 8.1 角色分工建议

| 角色 | 负责内容 |
|------|---------|
| 后端工程师 A | 认证模块（S-01）、机构权限（S-02）、客户管理（S-03）、资料管理（S-04） |
| 后端工程师 B | AI识别服务（S-05）、报告作业台（S-06）、材料包导出（S-08） |
| 后端工程师 C | 银行资料管理（S-07）、会员套餐（S-09）、调用记录（S-10）、平台配置（S-11） |
| 前端工程师 A | B端PC工作台（B-01 ~ B-06） |
| 前端工程师 B | B端PC工作台（B-07 ~ B-12）、AI助手面板 |
| 小程序工程师 | C端客户入口（C-01 ~ C-07）+ 顾问移动端（M-01 ~ M-06） |
| AI工程师 | Prompt 工程、OCR 集成、LLM 调用封装、异步 Worker |
| DBA/架构 | 数据库建表、索引优化、COS 配置、部署架构 |

### 8.2 开发顺序（4个阶段）

#### 阶段一：基础设施（第1-2周）

**并行开发，优先完成，其他模块依赖此阶段**

- [x] 数据库建表（全部表结构）— `backend/src/database/migrations/001-initial-schema.sql`
- [x] 后端项目初始化（NestJS）— `backend/`，88 个 TS 文件
- [x] JWT 认证（S-01）：手机号登录 + 微信小程序登录
- [x] 机构与权限（S-02）：机构创建、员工账号、角色权限中间件
- [x] 文件上传工具封装（本地存储，COS 密钥填入 `.env` 后即可切换）
- [x] API 统一响应格式、错误码约定（ResponseInterceptor + AllExceptionsFilter）
- [x] 前端项目初始化（React + Vite，`webapp_b/`；小程序 `miniprogram_c/` 框架已有）
- [ ] **待完成**：后端连接真实 PostgreSQL 实测（需 `docker compose up` 验证）
- [ ] **待完成**：COS 真实密钥配置与联调（当前为本地存储 fallback）

**验收标准：** 能登录、能创建员工账号、能上传一个文件到 COS

---

#### 阶段二：核心业务主链路（第3-5周）

**按业务链路顺序开发：客户 → 资料 → AI识别 → 360画像**

- [x] **S-03** 客户管理 CRUD（列表、新建、详情、标签、跟进记录）— 后端已完成
- [x] **S-04** 资料管理（上传、分类、状态管理、缺口字段）— 后端已完成
- [~] **B-02/B-03** 前端客户列表 + 客户详情 — `webapp_b/src/pages/Customers/`、`CustomerDetail/` 框架已有，**尚未对接后端 API**
- [ ] **C-02** 小程序 C端资料上传 — `miniprogram_c/` 页面框架已有，**尚未实现上传逻辑**
- [x] **S-05** AI 识别服务（Bull 异步队列框架已完成，OCR/LLM 为 Mock）— **需接入真实 OCR API**
- [ ] **B-05** 前端 AI 资料识别页面（`webapp_b/src/pages/Parsing/` 已有框架，**尚未对接**）
- [x] 客户 360 画像聚合接口（`GET /customers/:id/overview`）— 后端已完成

**验收标准：** 完整走通「客户创建 → 上传资料 → AI识别 → 查看缺口分析」

---

#### 阶段三：报告作业台主链路（第6-8周）

**核心差异化功能，优先 B端PC，移动端后补**

- [x] **S-07** 银行资料管理（银行CRUD、产品CRUD、字段配置）— 后端已完成
- [ ] **B-06** 前端银行资料管理页面 — `webapp_b/src/pages/BankCenter/`、`BankDetail/` 框架已有，**尚未对接后端 API**
- [x] **S-06** 报告作业台：创建填表任务、AI填写、字段草稿存储 — 后端已完成
- [ ] AI填表 Prompt 工程 — **尚未实现**，`ai.service.ts` 中为 Mock 数据占位
- [ ] **B-04** 前端报告作业台 — `webapp_b/src/pages/Reports/`、`ReportDetail/` 框架已有，**尚未对接后端 API**
- [x] **S-08** 材料包导出（ZIP 框架已完成，archiver 集成）— **需联调实际文件下载流程**
- [ ] **B-01** 前端今日作业 Dashboard — `webapp_b/src/pages/Dashboard/` 框架已有，**尚未对接后端 API**
- [ ] **M-04/M-05** 顾问移动端银行材料整理 + AI 资料识别 — `miniprogram_advisor/` 框架已有，**尚未实现**

**验收标准：** 完整走通「选产品 → AI填表 → 顾问复核 → 导出 ZIP → 标记已提交」

---

#### 阶段四：商业化与管理功能（第9-10周）

- [x] **S-09/S-10** 会员套餐 + 调用记录（后端接口已完成）
- [ ] **B-08/B-09** 前端会员套餐 + 调用记录页面 — `webapp_b/src/pages/Settings/` 框架已有，**尚未对接**
- [x] **S-11** 平台配置：AI填表规则管理 — 后端已完成
- [ ] **B-07/B-10/B-11** 前端平台配置 + 机构账号 + API配置 — **尚未实现**
- [ ] **B-12** AI 助手右侧面板 — **尚未实现**（`webapp_b` 中有 AI 助手组件框架）
- [ ] **C-01/C-03/C-04/C-05/C-06/C-07** C端剩余页面（进度、对话历史、授权） — `miniprogram_c/` 页面框架已有，**尚未对接后端 API**
- [ ] **M-01/M-02/M-03/M-06** 顾问移动端剩余页面 — `miniprogram_advisor/` 框架已有，**尚未实现**
- [ ] 通知模块（S-13）：补件提醒、微信模板消息 — **尚未实现**

**验收标准：** 完整演示三端全流程，配额管理正常扣次，权限控制生效

---

### 8.3 关键接口优先级（前后端联调顺序）

```
P0（阶段二完成前必须就绪）:
  POST /auth/login
  POST /auth/wx-login
  GET/POST/GET /customers
  POST /documents/upload
  POST /ai/recognize
  GET /ai/recognize/:taskId

P1（阶段三完成前就绪）:
  GET /banks, /bank-products/:id/material-config
  POST /report-tasks
  GET /report-tasks/:id（含字段草稿）
  PUT /report-tasks/:id/fields/:fieldId
  POST /report-tasks/:id/export

P2（阶段四）:
  GET /institution/subscription
  GET /call-records
  GET/POST /ai-fill-rules
```

---

## 9. 环境与部署

### 9.1 开发环境

| 环境 | 说明 |
|------|------|
| 本地开发 | 后端本地跑服务，前端 `vite dev` 代理到后端；小程序用微信 DevTools 开发 |
| 测试环境 | 部署到腾讯云测试域名，小程序配置测试版 appid |
| 生产环境 | 腾讯云 CloudBase 或 CVM + Nginx |

### 9.2 微信小程序开发注意

- AppID：`wxc9ebcb4c289511eb`
- 开发目录：`miniprogram_c/`，用 **微信 DevTools** 打开（不要用 `npm run build`）
- 渲染器：Skyline，组件框架：glass-easel
- TypeScript 严格模式：所有页面/组件的 `wx.*` 调用需要有类型覆盖
- 自定义导航栏：已有 `components/navigation-bar/` 组件，新页面复用

### 9.3 合规检查清单（上线前必过）

- [ ] 所有 AI 结果页面底部有合规提示「AI输出仅供参考，最终以银行及持牌金融机构审核为准」
- [ ] 不存在「承诺额度」「保证通过」等词语
- [ ] 客户征信授权有签署记录留存
- [ ] 所有数据操作有操作人+时间留痕（follow_up_records + call_records）
- [ ] 客户资料访问受数据范围权限控制（data_scope 字段）
- [ ] 导出操作记录到 call_records 并扣减配额

---

---

## 10. 当前进度与待完成事项

> 更新日期：2026-06-18

### 10.1 整体完成度

| 层 | 状态 | 说明 |
|----|------|------|
| 后端 API（NestJS） | ✅ 框架完成 | 88 个 TS 文件，所有接口已实现，TypeScript 编译通过 |
| PostgreSQL 数据库 | ✅ 框架完成 | 23 张表，DDL 迁移文件已就绪，尚未在真实 PG 实例上验证 |
| B端PC工作台前端 | 🟡 页面框架已有 | `webapp_b/src/pages/` 下各页面已有组件骨架，**均未对接后端 API** |
| C端微信小程序 | 🟡 页面框架已有 | `miniprogram_c/` 各页面已有，**均未对接后端 API** |
| 顾问移动端小程序 | 🟡 页面框架已有 | `miniprogram_advisor/` 各页面已有，**均未对接后端 API** |
| AI 能力层 | 🔴 Mock 占位 | OCR 识别、LLM 填表均为 Mock，需接入真实 API |
| 通知模块 | 🔴 未实现 | 微信模板消息、站内补件提醒均未实现 |

### 10.2 后端待完成事项

| 优先级 | 事项 | 文件位置 | 说明 |
|--------|------|---------|------|
| P0 | PostgreSQL 实例验证 | `backend/docker-compose.yml` | 运行 `docker compose up`，跑迁移，验证所有 TypeORM 实体同步 |
| P0 | 种子数据执行 | `backend/src/database/seed.ts` | `npx ts-node src/database/seed.ts` 写入初始套餐/机构/账号/银行数据 |
| P0 | 微信登录真实对接 | `backend/src/modules/auth/auth.service.ts` | 填入真实 `wx.jscode2session` 请求逻辑（当前有 HTTP 调用框架） |
| P1 | COS 文件上传 | `backend/src/modules/upload/upload.service.ts` | 填入腾讯云 COS SDK 配置，替换本地存储 fallback |
| P1 | OCR 资料识别接入 | `backend/src/modules/ai/ai.processor.ts` | 替换 Mock，接入真实 OCR API（如腾讯云 OCR / 百度 OCR） |
| P1 | AI 填表 Prompt | `backend/src/modules/reports/reports.service.ts` | 实现 `triggerAiFill()` 调用 LLM，生成 `report_field_drafts` 记录 |
| P1 | ZIP 导出联调 | `backend/src/modules/reports/reports.service.ts` | 完善 `exportPackage()`：拉取 COS 文件，archiver 打包，上传回 COS |
| P2 | 配额扣减中间件 | `backend/src/modules/memberships/memberships.service.ts` | 在 AI 识别 / 填表 / 导出前统一检查并扣减 `quota_used` |
| P2 | 微信模板消息 | 新建 `backend/src/modules/notifications/` | 补件提醒、提交银行通知 |
| P2 | 数据范围（data_scope）细化 | `backend/src/modules/customers/customers.service.ts` | 当前 `team` 数据范围仅按 advisor_id 过滤，需完善团队成员关系 |

### 10.3 前端待完成事项

#### B端PC工作台（`webapp_b/`）

所有页面框架已在 `webapp_b/src/pages/` 下，需逐一对接后端 API：

| 页面 | 目录 | 主要待完成工作 |
|------|------|------------|
| Dashboard（B-01） | `pages/Dashboard/` | 调用 `/report-tasks`、`/customers` 聚合展示，任务卡片数据真实化 |
| 客户列表（B-02） | `pages/Customers/` | 对接 `GET /customers`，搜索/筛选/分页参数传递 |
| 客户详情（B-03） | `pages/CustomerDetail/` | 四个 Tab 各对接对应接口；资料上传组件 |
| 报告作业台（B-04） | `pages/Reports/`、`ReportDetail/` | 字段复核 UI、来源指示图、逐字段修改提交 |
| AI 资料识别（B-05） | `pages/Parsing/` | 触发识别、轮询任务状态、展示缺口分析结果 |
| 银行资料管理（B-06） | `pages/BankCenter/`、`BankDetail/` | 银行/产品/字段配置的增删改查 |
| 平台配置（B-07） | `pages/Settings/`（待新增子页） | AI填表规则管理 |
| 会员套餐（B-08） | `pages/Settings/` | 套餐展示、用量进度条 |
| 调用记录（B-09） | `pages/Settings/` | 对接 `GET /call-records`，分页/筛选 |
| 机构账号（B-10） | `pages/Settings/` | 员工列表、新增、权限配置 |
| AI 助手面板（B-12） | `webapp_b/src/components/` | 自然语言查客户 → 调用 `/c/chat`，生成补件清单 |

#### C端微信小程序（`miniprogram_c/`）

| 页面 | 目录 | 主要待完成工作 |
|------|------|------------|
| 首页/AI对话（C-01） | `pages/chat/` | 对接 `POST /c/session`、`POST /c/chat`，流式回复展示 |
| 资料上传（C-02） | `pages/index/` 或新增 | 对接 `POST /c/documents/upload`，拍照/相册选取 |
| 办理进度（C-03） | `pages/progress/` | 对接 `GET /c/progress` |
| 联系顾问（C-04） | `pages/mine/` | 顾问联系方式展示 |
| 授权管理（C-05） | `pages/auth/` | 授权签署流程，写入 `customer_authorizations` |
| 历史对话（C-06） | `pages/history/` | 对接会话历史接口 |
| 我的资料（C-07） | `pages/mine/` | 资料列表、状态展示 |

#### 顾问移动端（`miniprogram_advisor/`）

| 页面 | 目录 | 主要待完成工作 |
|------|------|------------|
| 顾问AI对话（M-01） | `pages/chat/` | 对接 AI 助手接口，自然语言查客户 |
| 客户列表（M-03） | `pages/clients/` | 对接 `GET /customers`（顾问数据范围） |
| 银行材料整理（M-04） | `pages/bank/` | 对接银行产品资料配置接口 |
| AI 资料识别（M-05） | `pages/index/` 或新增 | 拍照识别，对接 `POST /ai/recognize` |
| 我的（M-06） | `pages/mine/` | 用量统计，对接 `/institution/subscription` |

### 10.4 环境验证清单（上线前）

- [ ] `docker compose up` 启动 PostgreSQL + Redis，迁移执行无报错
- [ ] `npm run seed` 种子数据写入正常，admin 账号可登录
- [ ] 所有 P0 接口（见 8.3 节）用 Postman/Swagger 验证通过
- [ ] 微信小程序 appid `wxc9ebcb4c289511eb` 配置后端服务域名白名单
- [ ] COS Bucket 配置跨域规则，允许小程序域名
- [ ] 合规检查清单（见 9.3 节）全部通过

---

*文档基于 prototype/index.html 及 docs/韬纪元AI_公网功能清单_20260615/ 生成，如有字段或业务规则变更请同步更新本文档。*
